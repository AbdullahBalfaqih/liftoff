import AddMoneyPanel from "@/components/dashboard/add-money";

export default function AddPage() {
  return (
    <div className="mx-auto flex w-full max-w-7xl flex-col gap-6">
      <AddMoneyPanel />
    </div>
  );
}
